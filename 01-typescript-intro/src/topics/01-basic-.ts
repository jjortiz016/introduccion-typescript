const name = 'jhon'; //si sabemos que no va a cambia le ponemos const
let hpPoinst: number | 'FULL' =95;
const isAlive: boolean= true;

hpPoinst= 'FULL'

console.log({name, hpPoinst, isAlive});
export{}
